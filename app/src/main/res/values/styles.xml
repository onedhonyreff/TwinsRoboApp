<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorblue</item>
        <item name="colorPrimaryDark">@color/colorblue</item>
        <item name="colorAccent">@color/colorblue</item>

<!--        <item name="android:windowFullscreen">false</item>-->
<!--        <item name="android:windowContentOverlay">@null</item>-->
<!--        <item name="android:fitsSystemWindows">false</item>-->
    </style>
    
    <style name="AppTheme.CustomNavigationBar">
        <item name="android:navigationBarColor">@color/SysNavBacground</item>
    </style>

    <style name="Widget.BottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/msmedium</item>
    </style>

    <style name="AppTheme.Popup">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    <style name="MyCustomRadioButton" parent="TextAppearance.AppCompat">
        <item name="android:paddingStart">6dp</item>
        <item name="android:paddingEnd">6dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@drawable/selectable_text_color_checked_state</item>
        <item name="android:background">@drawable/selectable_text_bg_color_checked_state</item>
        <item name="fontFamily">@font/msmedium</item>
        <item name="android:textSize">14dp</item>
    </style>

    <style name="RatingBar" parent="AppTheme">
        <item name="android:minHeight">10dp</item>
        <item name="android:maxHeight">10dp</item>
        <item name="colorControlActivated">@android:color/holo_orange_dark</item>
        <item name="colorControlNormal">#888888</item>
    </style>

</resources>
